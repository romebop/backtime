{
  "name": "backtime-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "A monorepo for backtime application",
  "main": "index.js",
  "scripts": {
    "dev": "pnpm --filter server run dev",
    "build": "pnpm --filter @backtime/types build && pnpm --filter client --filter server --parallel run build",
    "clean": "rm -rf apps/client/dist apps/client/dist-types apps/server/dist packages/types/dist && find . -name tsconfig.tsbuildinfo -delete"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}